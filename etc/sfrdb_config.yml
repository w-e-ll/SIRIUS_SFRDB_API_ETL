env: UAT
paths:
  base_dir: /home/id956955/apps/sirius_sfrdb_api_etl
  input_dir: ${base_dir}/var/data
  log_dir:   ${base_dir}/var/log
load:
  db_list: [xxx, xxx]
  batch_size: 2000
  truncate: true     # or false
databases:
  xxx:
    host: xxx
    port: 1540
    sid:  xxx
  xxx:
    host: xxx
    port: 1540
    sid:  xxx
